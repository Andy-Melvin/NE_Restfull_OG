
import React from 'react';

interface SocialLoginProps {
  type: 'login' | 'signup';
}

const SocialLogin: React.FC<SocialLoginProps> = () => {
  // Component now empty as we're removing social login functionality
  return null;
};

export default SocialLogin;
